{
    "name": "blog",
    "scripts": {
        "start": "cd server && yarn start & cd client && yarn start",
        "test": "cd server && yarn install && yarn start & cd server && yarn install && yarn test"
    }
}